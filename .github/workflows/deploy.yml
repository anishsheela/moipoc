name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 77.90.40.64
          username: anish
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "."
          target: /var/www/moidemo.authentiverse.net
          rm: true
